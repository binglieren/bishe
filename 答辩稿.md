# AI 考研助手系统 — 答辩稿

---

## 一、项目背景与选题意义

各位老师好，我的毕业设计题目是 **"AI 考研助手系统"**。

近年来，考研报名人数持续攀升，2025年已超过388万人。考生在备考过程中面临三个核心痛点：

1. **信息过载**：市面辅导资料繁多，考生难以快速定位自己真正薄弱的知识点。
2. **答疑困难**：遇到不会的题目缺少即时解答，人工答疑成本高、响应慢。
3. **个性化不足**：传统培训一刀切，无法根据每个考生的知识掌握情况提供精准的学习建议。

针对以上痛点，我设计并实现了一款基于大语言模型和 RAG（检索增强生成）技术的 AI 考研助手系统。核心思路是：**让考生上传自己的学习资料构建私有知识库，系统结合大模型实现智能问答与个性化学习路径规划**。

---

## 二、技术选型

系统采用 **前后端分离** 的微服务架构：

| 层级 | 技术栈 |
|------|--------|
| **前端** | React 18 + Vite + TypeScript + Ant Design（Web端/管理后台）+ React Native + Expo（移动端） |
| **后端** | Spring Boot 3.2.5 + Java 17 + Spring Security + JWT |
| **数据层** | PostgreSQL 16 + pgvector 向量扩展 + Redis 7 缓存 |
| **AI 框架** | LangChain4j（Agent 编排、嵌入、对话记忆） |
| **LLM 服务** | DeepSeek（对话 / 推理）、通义千问（多模态 / 嵌入 / 语音 / TTS） |
| **容器化** | Docker + Docker Compose，一键部署 5 个服务 |

---

## 三、系统架构设计

系统由 **4 个后端服务** + **3 个前端** 构成：

```
┌─────────────────────────────────────────────────────┐
│  前端层                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐      │
│  │ Web 用户端 │  │ 管理后台  │  │ React Native  │      │
│  └─────┬─────┘  └────┬─────┘  └──────┬───────┘      │
│        │              │               │               │
├────────┼──────────────┼───────────────┼───────────────┤
│  网关层 │              │               │               │
│        └──────────────┼───────────────┘               │
│                kaoyan-host (8088)                     │
│              Spring Boot 核心 API                      │
│      ┌─────────────────────────────────────┐         │
│      │     Agent 调度层 (Supervisor)          │         │
│      │  ┌───────┐ ┌──────┐ ┌────────┐ ┌──────────┐ │
│      │  │ TUTOR │ │ PLAN │ │TRACKER │ │RECOMMEND │ │
│      │  └───┬───┘ └──┬───┘ └───┬────┘ └────┬─────┘ │
│      └──────┼────────┼────────┼────────────┼────────┘
│             │        │        │            │          │
├─────────────┼────────┼────────┼────────────┼──────────┤
│  MCP 微服务 │        │        │            │          │
│  ┌──────────┴──┐ ┌───┴────────┴──┐ ┌──────┴────────┐ │
│  │  RAG Server │ │Question Server│ │Knowledge Server│ │
│  │   (8091)    │ │   (8092)      │ │    (8093)      │ │
│  │ 向量检索     │ │ 题库推荐      │ │ 知识图谱·诊断  │ │
│  └──────┬───────┘ └──────┬────────┘ └──────┬─────────┘ │
├─────────┼────────────────┼─────────────────┼───────────┤
│  数据层 │                │                 │           │
│  ┌──────┴────────────────┴─────────────────┴──────────┐│
│  │          PostgreSQL 16 + pgvector                   ││
│  │          Redis 7 (缓存 / Session)                   ││
│  └────────────────────────────────────────────────────┘│
└───────────────────────────────────────────────────────┘
```

架构设计的关键点：

- **MCP 微服务拆分**：将 RAG 检索、题库服务、知识图谱按领域拆为独立微服务，通过 HTTP 调用解耦，各自可独立部署和扩展。
- **Agent 编排模式**：采用 Supervisor + 4 个 Specialist Agent 的协作模式，大模型根据用户意图自动路由到对应 Agent，每个 Agent 绑定不同的 MCP 工具集。
- **pgvector 向量存储**：直接在 PostgreSQL 中存储 1536 维文本嵌入向量，避免引入独立向量数据库，降低运维复杂度。

---

## 四、核心功能实现

### 4.1 AI 智能问答（RAG + Agent）

这是系统的**最核心功能**，技术流程如下：

1. **用户提问** → 前端通过 **SSE（Server-Sent Events）** 建立长连接，实现 token 级别的流式输出。
2. **Supervisor 意图识别** → 大模型判断用户意图，路由到 TUTOR/PLAN/TRACKER/RECOMMEND Agent。
3. **TUTOR Agent 执行 RAG 检索** → 调用 RAG Server 进行语义搜索：
   - pgvector 向量余弦相似度 Top-20 召回
   - 关键词 BM25 重排序，取 Top-5
   - 命中分块自动向上下各扩展 1 个邻近分块，保证上下文完整
   - 将检索到的参考资料注入 System Prompt
4. **历史压缩** → 当对话历史预估 Token 超过 12000 时，调用 LLM 自动摘要压缩。
5. **多知识库绑定** → 一个会话最多绑定 5 个知识库，支持动态增删。
6. **深度思考模式** → 支持 switch 切换，开启后模型输出分两路：reasoning（推理过程）+ content（最终回答），前端分区域展示。

此外还支持：
- **拍照解题**：上传图片 → 多模态 LLM（通义千问-VL）识别 + RAG 增强 → 返回解答。
- **语音交互**：STT（语音识别转文字）+ TTS（文字转语音），使用通义千问 Omni / CosyVoice 模型，全链路支持语音问答。

### 4.2 智能题库

- **LLM 自动打标**：题目创建后进入 pending 状态，后台异步调用大模型，自动识别每道题涉及的知识点标签（多标签分类）。
- **向量语义召回**：每道题通过 text-embedding-v4 生成 1536 维向量，存入 pgvector。用户答题后，基于当前题目向量做相似题推荐。
- **个性化推荐**：综合薄弱知识点的权重 + Jaccard 标签相似度 + 向量语义距离，加权排序推荐题目。
- **错题本**：自动收录错题，支持标记已解决。

### 4.3 知识库与文档管理

- 用户创建多个知识库（如：数学基础 / 政治时政 / 专业课），每个知识库存放多份 PDF/Word 文档。
- 文档上传后自动 **分块（Chunking）→ 向量嵌入 → 存储 pgvector**。
- 用户可单独开关某个文档在 RAG 检索中的启用状态。
- 删除知识库时级联清理所有关联的向量数据。

### 4.4 知识图谱与学习诊断

- 知识点以**树形结构**组织（科目 → 章节 → 小节 → 具体知识点）。
- 每次用户提交答案，系统自动更新该知识点对应的 **KnowledgeMastery**（正确次数 / 总次数 / 掌握度等级）。
- **薄弱点识别**：设定阈值（如正确率 < 60%），自动筛选薄弱知识点。
- **AI 诊断报告**：综合掌握度数据 + 知识图谱结构，调用 LLM 生成个性化的学习诊断报告，包括：
  - 当前学习状况总评
  - 薄弱知识模块详细分析
  - 短期学习计划建议
  - 推荐的练题方向

### 4.5 五阶段 LLM 可配置

系统的 LLM 调用按功能划分为 5 个独立阶段：

| 阶段 | 用途 | 默认提供商 |
|------|------|-----------|
| chat | 文本对话、标题生成、题目打标 | DeepSeek |
| multimodal | 拍照解题、图片推理 | 通义千问-VL |
| embedding | RAG 向量化、题目向量化 | text-embedding-v4 |
| audio | 语音识别 STT | 通义千问-Omni |
| tts | 语音合成 TTS | CosyVoice 3.5 |

每个阶段的管理员可在后台独立配置 API URL / Key / 模型名，用户也可在个人中心覆盖 chat 阶段的配置。

---

## 五、数据库设计

核心数据表：

| 表 | 说明 |
|----|------|
| users + user_profile | 用户认证与考研档案 |
| knowledge_base | 知识库容器 |
| document | 上传的文档（含处理状态） |
| question + question_option | 题库 |
| knowledge_point | 知识树节点 |
| knowledge_mastery | 用户-知识点掌握度（正确/总次数） |
| knowledge_point_focus | 用户收藏知识点 |
| wrong_answer_record | 错题记录 |
| chat_session + chat_message | 对话会话与消息 |
| session_kb_binding | 会话↔知识库多对多绑定 |
| system_api_config | 管理员全局 LLM 配置 |
| ai_config | 用户自定义 AI 配置 |

其中 **pgvector** 扩展为两个场景提供向量支持：
- `document_chunk` 的文本嵌入（RAG 检索）
- `question` 的题目嵌入（相似题召回）

---

## 六、创新点

1. **Supervisor + MCP Agent 架构**：不同于简单的单次 LLM 调用，系统实现了意图分发（Supervisor）+ 多 Agent 协作 + 工具链调用，Agent 能够自主决定调用哪个微服务工具来完成任务。

2. **RAG 检索增强的工程优化**：混合检索（向量 + 关键词）、分块扩展（±1）、历史压缩、多知识库绑定、文档级开关，这些工程细节显著提升了检索质量和回答的相关性。

3. **全链路语音交互**：从语音输入（STT）→ RAG 增强理解 → LLM 回复 → TTS 语音输出，实现了完整的语音问答闭环。

4. **LLM 驱动的题库智能化**：自动打标 + 向量嵌入 + 个性化推荐，将传统静态题库升级为语义化智能题库。

5. **知识图谱驱动的诊断**：不是简单的错题统计，而是基于知识点树形拓扑 + 用户掌握度数据，由 LLM 生成有深度、可操作的学习诊断报告。

---

## 七、系统演示要点（建议演示顺序）

1. **登录注册** → 填写考研档案
2. **上传学习资料** → 建知识库 → 上传 PDF → 观察处理状态
3. **AI 问答** → 新建对话 → 绑定知识库 → 提问 RAG 相关题目 → 观察引用来源
4. **流式深度思考** → 开启 thinking 模式 → 观察思考过程 + 最终回答
5. **智能题库** → 刷题 → 提交答案 → 查看错题本
6. **分析面板** → 查看掌握度 → 薄弱知识点 → 答题推荐
7. **知识图谱** → 知识树可视化 → 点击节点看详情 → 生成 AI 诊断报告
8. **移动端**（如做了）→ 展示跨平台一致性
9. **管理后台** → 用户管理 / 题库管理 / 全局 LLM 配置

---

## 八、总结与展望

### 已完成的工作

- 完成了前后端三端（Web + Admin + Mobile）的完整开发
- 实现 RAG 增强的 AI 对话、智能题库、知识图谱诊断三大核心业务线
- 搭建了 Supervisor Agent + MCP 工具链的智能化架构
- 集成多种 LLM，实现五阶段可配置的模型调度

### 未来改进方向

- 引入更丰富的混合检索策略（如 Embedding + BM25 + 倒排索引联合召回）
- 支持更多文档格式（Markdown、HTML、代码文件）
- 加入多轮对话的强化学习反馈（RLHF）优化回答质量
- 知识图谱前端可视化（D3.js / ECharts 关系图）
- 引入消息队列（如 RabbitMQ）做异步文档处理，提升大文件上传体验

---

## 九、致谢

以上就是我的毕业设计汇报，感谢各位老师的聆听，请各位老师批评指导。
